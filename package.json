{
    "name": "root",
    "private": true,
    "scripts": {
        "test": "lerna run test --parallel",
        "start": "lerna run start",
        "lint": "lerna run lint",
        "bootstrap": "lerna bootstrap"
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    }
}
